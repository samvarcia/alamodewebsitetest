import localFont from "next/font/local";
// import { Inter } from "next/font/google";
import "./globals.css";

// const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "Á LA MODE",
  description: "Generated by create neLocation à la mode is a model exclusive event series created to foster a sense of community within the often isolating modeling industry.xt app",
};
const futura = localFont({
  src: [
    {
      path: "./futuralight.ttf",
      weight: "300",
      style: "normal",
    },
    {
      path: "./futuraregular.ttf",
      weight: "400",
      style: "normal",
    },
  ],
});
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={futura.className}>{children}</body>
    </html>
  );
}
